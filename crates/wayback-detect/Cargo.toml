[package]
name = "wayback-detect"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"  # or current, via docs.rs.[web:73]
thiserror = "1"
hex = "0.4"
schemars = "0.8"
