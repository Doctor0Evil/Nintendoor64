[package]
name = "capi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
core-ecs = { path = "../core-ecs" }
arena-shooter-core = { path = "../arena-shooter-core" }

[build-dependencies]
cbindgen = "0.26"
